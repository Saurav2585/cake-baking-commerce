# Canonical data location

The Phase 5B canonical JSON records are the named files one level above this directory. `Catalog_Data_Index.json` provides stable relative pointers and the acceptance counts; this directory intentionally contains no duplicate source data.
