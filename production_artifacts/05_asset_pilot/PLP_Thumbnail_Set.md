# PLP Thumbnail Set

The three SKU exports each have 640×640 and 320×320 WebP versions. `asset_pf5a_plp_parent_640_v1.webp` represents the parent with the 500 g pack only when the card explicitly says “3 pack sizes” and uses **Select options**, never direct Add. Pack weight and SKU are repeated as HTML in variant-specific contexts.

At the tested 160 px placement, weight, silhouette and one/two/four bar cue are visible. At 96 px, small label copy is not relied on; accessible names and card copy carry product identity and pack basis. Variant differentiation survives grayscale because it uses size, written weight and bar count. The fallback keeps the card functional without imagery.
