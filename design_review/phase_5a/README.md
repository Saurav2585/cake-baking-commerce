# Phase 5A Placement Review

Static, non-production placement harness for the bounded visual asset pilot. Serve `design_review/` locally and open `/phase_5a/?view=home`. Routes: `home`, `department`, `plp`, `pdp`, `recipe`, `bridge`, `failure`. It contains no production commerce, persistence, payment, database or generated packaging text.
